import React from 'react';

const RadarChart = ({ data }) => {
  return (
    <div className="radar-chart">
      {/* Implement radar chart using Chart.js or Recharts */}
    </div>
  );
};

export default RadarChart;
